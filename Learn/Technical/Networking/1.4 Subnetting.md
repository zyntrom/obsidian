## 📄 **1.4 Subnetting**

**Subnetting = dividing one large network into smaller networks**

### 🔹 Why subnet?

- Better performance
- Access control & segmentation
- Reduce broadcast traffic
- **Security advantage** — breaches get contained per subnet

---

### 🔹 Subnet Mask Meaning

Example: `255.255.255.0` = `/24`

|Mask|Hosts|Networks|
|---|---|---|
|/24|254 hosts|common|
|/25|126 hosts|2 subnets|
|/26|62 hosts|4 subnets|
|/30|2 hosts|point-to-point|

Formula:

`Number of hosts = 2^(32 - subnet bits) - 2`

---

### 🔹 Quick Subnetting Practice

Example subnet: `10.0.0.0 / 27`

|Field|Result|
|---|---|
|Hosts|2^(32–27) – 2 = 30|
|No. of subnets in /24 space|256 / 32 = 8 subnets|
|Block size|32 IPs per subnet|

🧠 **Hacker Tip**  
When enumerating networks during a penetration test:

- `/24 subnet` = **254 targets to scan**
- `/16 subnet` = **65,000 targets** → need efficient scan techniques (`nmap` fast scan + ping sweep)