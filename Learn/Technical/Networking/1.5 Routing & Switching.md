## 📄 **1.5 Routing & Switching**

### 🔹 Switching (Layer 2 – Ethernet)

- Uses **MAC addresses** to forward traffic inside LAN
- Maintains **MAC Address Table**
- Sends traffic only to destination port (not broadcast)

💀 **Hacker use**

- **ARP spoofing → intercept LAN traffic**
- **MAC flooding → force switch into hub-mode → packet sniffing**

---

### 🔹 Routing (Layer 3 – IP)

- Moves packets **between networks**
- Uses **routing table**

Example routing table entry:

`Destination: 192.168.5.0 Mask: 255.255.255.0 Gateway: 192.168.1.1 Interface: eth0`

---

### 🔹 Default Gateway

Router IP that forwards packets **when destination is outside local network**

🧠 Attack use:

- If attacker becomes the **fake gateway** → full MitM

---

### 🔹 ARP (Address Resolution Protocol)

| Converts | IP → MAC |  
| Layer | 2 |  
| Weakness | No authentication |

💀 Exploitable for:

- ARP Poisoning
- Session Hijacking
- SSL-strip (if combined with MitM)

---

### 🔹 MAC Address

- Unique hardware identifier (Layer 2)
- Can be **spoofed**
- Tools: `macchanger`, `ifconfig`, `ip link`